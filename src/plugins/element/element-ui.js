/**
 * 按需引入element-ui
 */

import Vue from 'vue';
import { Button, Input, Select, Popover } from 'element-ui';
import './element-variables.scss';

Vue.use(Button);
Vue.use(Input);
Vue.use(Select);
Vue.use(Popover);
